#include "pch.h"
#include "CppUnitTest.h"
// #include "../KeyBoardBurgers/LevelController.h"
// #include "../KeyBoardBurgers/LevelController.cpp"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace TestLevelController
{
	TEST_CLASS(TestLevelController)
	{
	public:
		/*LevelController levelController;
		TEST_METHOD(TestCheckStatus)
		{
			Assert::IsTrue(levelController.checkStatus());
		}
		TEST_METHOD(GetandSetLevelNum)
		{
			levelController.setCurrentLevelNum(2);
			Assert::IsTrue(levelController.getCurrentLevelNum() == 2);
		}
		TEST_METHOD(GetHighScore)
		{
			Assert::IsTrue(levelController.getHighScore() == 0);
		}
		TEST_METHOD(IsBeat)
		{
			Assert::IsTrue(levelController.isBeat());
		}
		TEST_METHOD(SetIfHighScore)
		{
			Assert::IsTrue(!levelController.setIfHighScore());
		}*/

	};
}